import Nav from "./Navbar";

export default function Header() {
    const styles = {
        heading: {
            display: "flex",
            justifyContent: "flex-start",
        },
    };
    return <h1>Udit</h1>;
}
